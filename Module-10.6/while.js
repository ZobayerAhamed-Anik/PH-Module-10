const numbers = [74, 38, 30, 37, 10, 29, 39];

let i = 0;
while(i < numbers.length ){
    console.log('Index value', i);
    let item = numbers[i];
    console.log('Value of the item:', item);
    i++;
}